# Stub crate to satisfy Cargo manifest resolution.
# The real light-client-db-common lives in the upstream ckb-light-client repo.
# This stub is only needed because Cargo resolves all path dependencies
# (including cfg-gated wasm-only ones) during manifest loading.

[package]
name = "light-client-db-common"
version = "0.1.0"
edition = "2021"
